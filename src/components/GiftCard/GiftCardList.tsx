import { FlatList, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const GiftCardList = () => {
  return (
    <FlatList  />
     /*  <Text>GiftCardList</Text>
    </FlatList> */
  )
}

export default GiftCardList

const styles = StyleSheet.create({})